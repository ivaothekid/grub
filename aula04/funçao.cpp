#include <bits/stdc++.h>
using namespace std;

string inverte(string s) {
	string ans = "";

	for(int i = s.size()-1; i >= 0; i--){
		ans = ans + s[i];
	}
	return ans;
}

int main(){
	ios_base::sync_with_stdio(false);

	

	return 0;
}
